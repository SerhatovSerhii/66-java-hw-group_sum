package ait.homework.numbers.model;

public class ParalelStreamGroupSum extends GroupSum{
    public ParalelStreamGroupSum(int[][] numberGroups) {
        super(numberGroups);
    }

    @Override
    public int computeSum() {
        //  TODO HOMEWORK: reduce(посчитать) sum numbers of numbersGroups, use Arrays.stream(.....).paralel()

        return 0;
    }
}
